# RetroBoard

Sprint retrospektif board uygulaması.

## Yapı

- `frontend/` — React (Vite)
- `backend/` — Node.js + Express

## Kurulum

```bash
# Backend
cd backend && npm install && npm run dev

# Frontend
cd frontend && npm install && npm run dev
```
